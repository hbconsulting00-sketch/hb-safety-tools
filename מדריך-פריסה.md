# מדריך פריסה מלא — HB Safety Tools
### מהקובץ המקומי לאתר אינטרנט אמיתי

---

## מה נצטרך?
- חשבון Anthropic (לאנשים שיש להם גישה לקלוד)
- חשבון GitHub (כבר יש לך ✓)
- חשבון Supabase (חינמי, ניצור עכשיו)
- חשבון Vercel (חינמי, ניצור עכשיו)
- **כרטיס אשראי? לא.** הכל בחינם לשימוש עסקי קטן.

---

# שלב 1 — מפתח Claude API

> זה המפתח שמאפשר לכלי להשתמש ב-AI של Anthropic (Claude).

**1.1** פתחי דפדפן ולכי לכתובת:
```
https://console.anthropic.com/settings/keys
```

**1.2** התחברי עם המייל שאיתו יש לך חשבון Anthropic.

**1.3** לחצי על הכפתור:
```
Create Key
```

**1.4** תני לו שם (לדוגמה `hb-safety-tools`) ← לחצי **Create Key**.

**1.5** יופיע המפתח שנראה כך:
```
sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

**1.6** לחצי על **Copy** ← שמרי אותו בפתק (מחוץ לקוד, לא ב-GitHub!).

> ⚠️ המפתח מוצג רק פעם אחת. אם לא שמרת — תמחקי ותצרי חדש.

---

# שלב 2 — Supabase (מסד הנתונים של המאגר המשותף)

> כאן ישמרו המושגים שהצוות אוסף — כולם רואים את אותו מאגר.

**2.1** לכי לכתובת:
```
https://supabase.com
```

**2.2** לחצי:
```
Start your project
```

**2.3** לחצי:
```
Continue with GitHub
```
(הכי נוח — מחבר הכל בלחיצה אחת)

**2.4** לאחר ההתחברות, לחצי:
```
New project
```

**2.5** מלאי את הפרטים:

| שדה | מה למלא |
|-----|---------|
| **Organization** | ישאר כמו שהוא |
| **Name** | `hb-safety-tools` |
| **Database Password** | המצאי סיסמה חזקה ושמרי (לא נצטרך אותה בהמשך, רק אם תרצי לגשת ל-DB ישירות) |
| **Region** | `West EU (Ireland)` — הכי קרוב לישראל |
| **Pricing plan** | `Free` ✓ |

**2.6** לחצי:
```
Create new project
```

⏳ חכי 2-3 דקות עד שהפרויקט מוכן (תראי אנימציה).

---

## יצירת הטבלה

**2.7** בתפריט השמאלי לחצי על:
```
SQL Editor
```
(סמל שנראה כמו טרמינל / שורת קוד)

**2.8** תיפתח חלונית עם שטח כתיבה לבן. אם יש שם טקסט — מחקי הכל.

**2.9** העתיקי את הקוד הבא והדביקי אותו:

```sql
CREATE TABLE saved_terms (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  term        TEXT NOT NULL,
  definition  TEXT,
  practical   TEXT,
  created_at  TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE saved_terms ENABLE ROW LEVEL SECURITY;
CREATE POLICY "allow_all" ON saved_terms FOR ALL USING (true) WITH CHECK (true);
```

**2.10** לחצי על הכפתור הירוק:
```
Run
```

**2.11** תראי בתחתית המסך:
```
Success. No rows returned
```
← מושלם! הטבלה נוצרה.

---

## העתקת המפתחות מ-Supabase

**2.12** בתפריט השמאלי, גללי לתחתית ולחצי על:
```
Project Settings
```
(סמל גלגל שיניים)

**2.13** בתפריט הפנימי לחצי על:
```
API
```

**2.14** עכשיו תראי שני דברים שצריך להעתיק:

**דבר ראשון — Project URL:**
```
https://abcdefghijk.supabase.co
```
← לחצי **Copy** לידו ← שמרי בפתק.

**דבר שני — service_role key:**
גללי למטה לכותרת **Project API Keys** ← שורה שכתוב בה **service_role** ← לחצי **Reveal** ← לחצי **Copy**:
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOi...
```
← שמרי גם זה בפתק.

> ⚠️ ה-service_role key הוא מפתח מלא — אל תחשפי אותו לאף אחד.

---

# שלב 3 — העלאת הקוד ל-GitHub

**3.1** לכי לכתובת:
```
https://github.com/new
```

**3.2** מלאי:
- **Repository name:** `hb-safety-tools`
- בחרי: `Private` (כדי שהקוד לא יהיה ציבורי)
- **אל תסמני** שום דבר נוסף (לא README, לא gitignore)

**3.3** לחצי:
```
Create repository
```

---

## פתיחת PowerShell

חפשי בסרגל החיפוש של Windows:
```
PowerShell
```
← לחצי עליו.

---

## בדיקה שיש Git ו-Node.js

הדביקי כל פקודה ולחצי Enter:

```
git --version
```
אם מקבלת `git version 2.x` ← מעולה.
אם לא ← הורידי מ: `https://git-scm.com/download/win`

```
node --version
```
אם מקבלת `v18.x` ומעלה ← מעולה.
אם לא ← הורידי מ: `https://nodejs.org` ← לחצי **LTS**.

---

## העלאת הקוד

**3.4** נווטי לתיקיית הפרויקט (הדביקי ולחצי Enter):
```
cd "C:\Users\michal\Desktop\תוצרי AI\safety tool\hb-safety-web"
```

**3.5** הריצי את הפקודות הבאות **אחת אחרי השנייה**:

```
git init
```
```
git add .
```
```
git commit -m "first commit"
```

**3.6** חברי ל-GitHub — **החלפי** את `YOUR_USERNAME` בשם המשתמש שלך ב-GitHub:
```
git remote add origin https://github.com/YOUR_USERNAME/hb-safety-tools.git
```
```
git branch -M main
```
```
git push -u origin main
```

> 💡 אם GitHub מבקשת שם משתמש ← הכניסי את שם המשתמש שלך.
> לסיסמה ← לא להכניס את הסיסמה הרגילה! צריך **Personal Access Token**:
> לכי ל: `https://github.com/settings/tokens/new`
> ← סמני `repo` ← לחצי **Generate token** ← העתיקי והדביקי במקום הסיסמה.

**3.7** לאחר הסיום, כנסי ל-GitHub ותאמתי שהקוד הועלה ✓

---

# שלב 4 — פריסה ב-Vercel

**4.1** לכי לכתובת:
```
https://vercel.com
```

**4.2** לחצי:
```
Sign Up
```
← ואז:
```
Continue with GitHub
```

**4.3** לאחר ההתחברות תגיעי ל-Dashboard. לחצי:
```
Add New...
```
← ואז:
```
Project
```

**4.4** תראי את ה-repos מ-GitHub. מצאי `hb-safety-tools` ולחצי:
```
Import
```

**4.5** תגיעי למסך הגדרות. **לפני שלוחצת Deploy** — גללי למטה לחלק:
```
Environment Variables
```

---

## הוספת המפתחות ב-Vercel

זה המקום המאובטח שבו המפתחות נשמרים — **לא** ב-GitHub, **לא** בקוד.

הוסיפי **3 משתנים**. בכל פעם: מלאי Name ← Value ← לחצי Add:

---

**משתנה 1:**

Name:
```
ANTHROPIC_API_KEY
```
Value: ← המפתח מ-Anthropic שהעתקת בשלב 1 (מתחיל ב-`sk-ant-api03-`)

לחצי **Add**

---

**משתנה 2:**

Name:
```
NEXT_PUBLIC_SUPABASE_URL
```
Value: ← ה-Project URL מ-Supabase (מתחיל ב-`https://`)

לחצי **Add**

---

**משתנה 3:**

Name:
```
SUPABASE_SERVICE_ROLE_KEY
```
Value: ← ה-service_role key מ-Supabase (מתחיל ב-`eyJhbGci`)

לחצי **Add**

---

**4.6** לאחר ה-3 משתנים, לחצי:
```
Deploy
```

**4.7** ⏳ חכי 2-3 דקות — תראי לוג של בנייה.

**4.8** כשמסתיים:
```
🎉 Congratulations! Your project has been successfully deployed.
```

**4.9** לחצי על:
```
Visit
```

האתר שלך פתוח! הכתובת תיראה כמשהו כזה:
```
https://hb-safety-tools.vercel.app
```

---

# עדכונים עתידיים

בכל פעם שתרצי לשנות משהו בכלי:
1. ערכי את הקוד בתיקייה המקומית
2. ב-PowerShell (מאותה תיקייה):
```
git add .
git commit -m "תיאור השינוי"
git push
```
3. Vercel יפרוס אוטומטית תוך 2 דקות ✓

---

# סיכום — מה שמרת בפתק

| מה זה | מאיפה | שם המשתנה ב-Vercel |
|--------|--------|-------------------|
| מפתח Claude | console.anthropic.com | `ANTHROPIC_API_KEY` |
| URL של Supabase | Supabase → Settings → API | `NEXT_PUBLIC_SUPABASE_URL` |
| Service role key | Supabase → Settings → API | `SUPABASE_SERVICE_ROLE_KEY` |

---

# שאלות נפוצות

**האם זה עולה כסף?**
Vercel — חינמי. Supabase — חינמי עד 500MB. Claude API — מחויב לפי שימוש, אבל לכלי פנימי בחברה הסכומים קטנים מאוד (כמה שקלים לחודש).

**מי יכול לגשת לאתר?**
כל מי שיש לו את הכתובת. אם תרצי להגביל — ניתן להוסיף סיסמה דרך Vercel (Password Protection, בהגדרות הפרויקט).

**האם המפתחות מאובטחים?**
כן לחלוטין. הם נשמרים רק ב-Vercel ולעולם לא נשלחים לדפדפן. המשתמשים רואים רק את ממשק האתר.

**אני נתקלת בשגיאה — מה עושים?**
שלחי לי את הודעת השגיאה ואני עוזרת 🙂
